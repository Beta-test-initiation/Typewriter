// Copy this file to config.js and fill in your real values.
// config.js is gitignored — never commit your actual keys.
//
// How to find each value in EmailJS (emailjs.com):
//   PUBLIC_KEY   → Account (top-right avatar) → API Keys → Public Key
//   SERVICE_ID   → Email Services → click your service → Service ID (e.g. service_xxxxxxx)
//   TEMPLATE_ID  → Email Templates → click your template → Template ID (e.g. template_xxxxxxx)
//
// SITE_URL: set this once you've deployed to GitHub Pages.

window.TYPEWRITER_CONFIG = {
  EMAILJS_PUBLIC_KEY:  'YOUR_PUBLIC_KEY',
  EMAILJS_SERVICE_ID:  'YOUR_SERVICE_ID',
  EMAILJS_TEMPLATE_ID: 'YOUR_TEMPLATE_ID',
  SITE_URL:            'YOUR_SITE_URL',  // e.g. https://username.github.io/typewriter
};
